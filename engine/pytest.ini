[pytest]
# Scope collection to the tests/ directory so production modules (e.g. the
# function legend.providers.test_connection) are never picked up as test cases.
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q -ra
filterwarnings =
    ignore::DeprecationWarning
