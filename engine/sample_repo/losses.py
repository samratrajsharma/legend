"""Loss functions for training."""


def cross_entropy(preds, targets):
    """Standard cross-entropy loss."""
    return 0.0


def focal_loss(preds, targets, gamma=2.0):
    """Focal loss, used to address class imbalance in dense detection."""
    return 0.0
