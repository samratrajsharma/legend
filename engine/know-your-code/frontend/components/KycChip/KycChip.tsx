import './KycChip.css';
// Small product chip shown top-right of every KYC page-header (violet gradient per brief).
export default function KycChip() {
  return <span className="kyc-chip">Legend</span>;
}
